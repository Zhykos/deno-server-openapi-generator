  import { join } from "https://deno.land/std@0.125.0/path/mod.ts";
  import makeloc from 'https://x.nest.land/dirname@v1.1.2/mod.ts'

const { __dirname } = makeloc(import.meta)

export const config = {
  ROOT_DIR: __dirname,
  URL_PORT: {{serverPort}}{{^serverPort}}3000{{/serverPort}},
  URL_PATH: '{{serverHost}}{{^serverHost}}http://localhost{{/serverHost}}',
  BASE_VERSION: '{{contextPath}}{{^contextPath}}v2{{/contextPath}}',
  CONTROLLER_DIRECTORY: path.join(__dirname, 'controllers'),
  PROJECT_DIR: __dirname,
};

export const config2 = {
  OPENAPI_YAML: join(config.ROOT_DIR, "api", "openapi.yaml"),
  FULL_PATH: `${config.URL_PATH}:${config.URL_PORT}/${config.BASE_VERSION}`,
  FILE_UPLOAD_PATH: join(config.PROJECT_DIR, "uploaded_files"),
};
